<p id="notice"><%= notice %></p>
<center>
<div class = "sort_paginate_ajax"><%= render 'plates' %></div>

<link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
<br>
<% if current_client %>
  <% if current_client.id == @restaurante.client_id %>
    <%= link_to "<i class=material-icons md-dark>add_box</i> Agregar Plato".html_safe, new_restaurante_plate_path(@restaurante) %>
  <% end %>
<% end %>
